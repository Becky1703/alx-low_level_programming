#include "main.h"

/**
 * _isdigit - prints digits from 0 to 9
 * @c: is the int to check through
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c);
{
return (c >= '0' &&  c <= '9');
}
